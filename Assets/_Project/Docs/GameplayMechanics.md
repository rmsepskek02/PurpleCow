# GameplayMechanics.md

이 문서는 UI 규칙이 아니라 "게임 내 구체적인 알고리즘/메커닉"을 기록하는 문서입니다.
UI 배치나 연출이 아닌, 게임플레이 로직 자체의 동작 방식(조준, 발사, 충돌, 귀환 등)을 다룹니다.
향후 이런 종류의 알고리즘/메커닉이 추가로 확정되면 이 문서에 섹션을 계속 추가합니다.
원본 게임(통통 디펜스: 핀볼 마스터) 실제 플레이 경험을 보유한 사용자가 확인해준 내용을 기준으로 작성되었습니다.

---

## 1. 볼 발사 및 궤도 시스템

원본 게임 실제 플레이어(사용자)가 확인해준 볼 발사/궤도 메커닉입니다.

- 플레이어가 화면을 **터치하는 순간** 조준 방향이 즉시 정해진다. 드래그 후 손을 떼는 시점이 아니라, 터치를 시작하는 시점에 조준이 시작된다.
- 조준 중에는 점선 형태의 예상 경로가 표시되며, 정확히 2단계 구간까지만 그려진다. 전체 반사 경로 전부를 보여주는 것이 아니다.
  1. 조준 방향으로 뻗어나가 **첫 번째 충돌 지점**까지 점선이 그려진다.
  2. 그 지점에서 **반사(튕겨나가는) 방향**으로 이어져 **두 번째 충돌 지점**까지 점선이 계속 그려진다.
  3. 두 번째 충돌 지점에는 **빨간 점(레드닷)**과 그 점을 감싸는 **둥근 원형 궤적선(고리 형태)**이 표시된다.
  4. 그 이후(세 번째 충돌 이후)의 경로는 표시되지 않는다.
- 드래그하면 궤적이 드래그 위치를 목표로 실시간으로 따라간다. 터치를 유지하는 동안 계속 갱신된다.
- 한 번 발사되어 날아간 볼은 플레이어가 더 이상 직접 제어할 수 없다. 벽/몬스터와의 충돌에 따라 물리적으로 방향이 바뀌며 이동한다.
- 볼은 최종적으로 화면 하단에서 튕겨 캐릭터 위치로 돌아온다.
- 캐릭터에게 돌아온 볼은 **그 시점의 최신 조준 방향**으로 자동으로 다시 발사된다. 재발사 시점에 정해진 방향을 사용하며, 최초 발사 당시의 방향이 아니다.
- 이 발사 → 충돌 이동 → 귀환 → 재발사 사이클이 계속 반복되며, 플레이어는 이 사이클 속에서 "다음 재발사 때 사용할 조준 방향"을 실시간으로 계속 조준할 수 있다.
- 볼의 이동 속도는 조준/조작과 무관하게 항상 일정하다.

### 구현 현황

관련 코드: `Assets/_Project/Scripts/Core/InputHandler.cs`, `Assets/_Project/Scripts/Ball/BallLauncher.cs`, `Assets/_Project/Scripts/Ball/Ball.cs`, `Assets/_Project/Scripts/Ball/TrajectoryPreview.cs`

- **(구현 완료)** 터치 즉시 조준 시작(`InputHandler.OnAimBegin`) + 실시간 드래그 추적 + 2단계 궤적 프리뷰(`TrajectoryPreview.cs`) + 볼이 캐릭터로 귀환 시 자동 재발사 사이클(`BallLauncher`/`Ball`의 로스터 모델)까지 `Assets/_Project/Docs/_Task/2026-07-01/21-15_ball-launch-mechanics/` task로 설계·구현·QA 완료 후 main에 반영됨(노말볼 5개 + 특수볼 최대 4종 로스터 구조 포함).

---

## 2. 몬스터 스폰 및 전진 시스템

원본 게임 실제 플레이어(사용자)가 확인해준 몬스터 스폰/전진 메커닉입니다.

- 한 웨이브가 시작되면 해당 웨이브에 소속된 몬스터가 **한 번에 전부** 스폰된다. 시간차를 두고 순차적으로 스폰되지 않는다.
- 스폰 위치는 필드 상단 그리드 영역 내에서 **랜덤하게 배치**된다. 이전 웨이브에서 어떤 위치가 점유되어 있었는지와 무관하게 매 웨이브 새로 랜덤 결정된다.
- 몬스터는 스폰 직후부터 **시간 경과에 따라 연속적으로(부드럽게)** 화면 하단을 향해 전진한다. 그리드 한 칸씩 딱딱 끊어지는 스텝 이동이 아니다.
- 전진은 볼 발사/귀환 사이클과 **무관한 독립적인 시간 흐름**에 따라 진행된다. 볼이 몇 개 남아있든, 귀환했든과 상관없이 몬스터는 계속 전진한다.
- 몬스터가 화면 하단(캐릭터 주변 경계선)에 도달하면 캐릭터 HP를 깎고 소멸한다. 캐릭터 HP 차감 및 경험치(XP) 처리는 [UIRules.md](UIRules.md) 섹션 10 "캐릭터 HP / 경험치 / 레벨 시스템"에 이미 정리되어 있으므로 이 문서에서는 중복 서술하지 않는다.
- 웨이브 클리어 조건은 해당 웨이브에 스폰된 몬스터를 **전멸**시키는 것이다. 전멸하면 다음 웨이브가 스폰된다.
- 게임의 목표는 몬스터 처치를 통한 생존이다.

### 구현 현황

관련 코드: `Assets/_Project/Scripts/Wave/WaveManager.cs`, `Assets/_Project/Scripts/Monster/MonsterBase.cs`

- **(구현 완료)** `WaveManager`의 `OnAllBallsReturned` 결합을 완전히 제거하고, `MonsterBase`가 매 프레임 스스로 `MonsterData.MoveSpeed` 기반 시간 연속 하강을 수행하도록 재설계함. `WaveManager.CheckGameOver()`(하단 도달 판정)도 매 프레임 체크로 전환. 냉동(`ApplyFreeze`)/슬로우(`ApplySlow`)도 턴 기반에서 초 단위 타이머로 전환 완료. `Assets/_Project/Docs/_Task/2026-07-01/21-15_ball-launch-mechanics/` task(볼 발사 메커닉 재설계)의 일환으로 함께 구현·QA 완료 후 main에 반영됨.

참고: 하단 도달 시 캐릭터 HP 차감 및 경험치 획득 로직(`CharacterManager.cs`)은 이미 `UIRules.md` 섹션 10 스펙대로 구현되어 있어 이번 TODO 범위에 포함하지 않는다.
